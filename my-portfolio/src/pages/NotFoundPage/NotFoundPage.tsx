import * as React from 'react'


// Props interface (if needed, you can define properties here)
export interface IAppProps {}

// Functional component
const App: React.FC<IAppProps> = (props) => {
  return (
    <>
      <p>I am the not found page</p>
    </>
  );
};

export default App;
